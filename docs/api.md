# API Reference

To view the API documentation, we recommend using the local cargo command:

```bash
cargo doc --no-deps --open
```

We will be adding this library to the public crates site in the future.